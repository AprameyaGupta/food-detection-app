import 'package:flutter/material.dart';

class CameraApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('CameraApp')),
    );
  }
}

final cameraApp = CameraApp();
